namespace WebApp_GKH.Helpers
{
    public static class Converter
    {
        /// <summary>
        /// 
        /// </summary>
        /// <param name="date"></param>
        /// <returns></returns>
        public static DateTime ConvertStringToDate(string dateStr)
        {
            var date = DateTime.Now;
            // конвертируем строку в дату(+ проверки)

            return date;
        }
    }
}
